<%- include("partials/header") -%>
    <h1>Home</h1>
    <p>
        <%=startingContent%>
    </p>
    <% for(var i=0;i<newListItem.length;i++){%>
        <div class="item">
            <h2>
                <%=newListItem[i].title%>
            </h2>
            <p>
                <%=newListItem[i].body.substring(0,100)+"..."%>
                    <a href="<%= " /posts/"+newListItem[i].title %>">Read More..</a>
            </p>
        </div>
        <% } %>
            <%- include("partials/footer") -%>